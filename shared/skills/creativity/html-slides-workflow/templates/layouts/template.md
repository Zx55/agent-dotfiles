# Slide NN — Layout

## 状态与本页目标

- 状态：提案 / 已确认 / 已实现 / 已渲染检查（只保留真实状态）。
- 本页唯一重点：观众需要记住的一句话。
- 与前后页的区别：本页新增什么，不重复什么。
- 相关文件：[HTML](../slides/slideNN.html)、[口播稿](../notes/slideNN.md)、[storyboard](../storyboard.md)。

## 画面上的文字

- 标题及需要强调的部分。
- 副标题（需要时）。
- 核心标签、公式或数据，以及必须保留的单位。
- 底部结论（需要时）。

只写画面需要的文字。全文口播放在 notes，不放在此处。颜色、字体、字号和字重引用 theme 中的语义变量。

## 构图草图

用一个简短线框图或空间描述说明主要区域、主视觉、阅读顺序和连接关系。选择符合本页内容的构图，不默认套用左右双栏或卡片网格。

## 区域与排版

画布默认沿用共享 16:9 模板。标题、副标题、底部结论和页码使用共享锚点，不另造一套坐标。以下只记录本页需要明确的区域。

| 区域 | 位置、尺寸或比例 | 内容与层级 | 对齐关系 |
| --- | --- | --- | --- |
| 主视觉 | 填写实际方案 | 图片 / SVG / 图表 | 与哪些元素对齐 |
| 解释区（如需） | 填写实际方案 | 标签 / 公式 / 简短文字 | 与主视觉的关系 |

涉及精确坐标时，注明是页面坐标还是 SVG 内部坐标。指出哪些区域移动、哪些留白增加、哪些图片保持原尺寸。优先调整结构和文案，不靠缩小字体解决拥挤。

## 素材与裁切

| 素材 | 本地路径或待提取来源 | 必须保留的内容 | 裁切与显示方案 |
| --- | --- | --- | --- |
| 按需填写 | ../assets/slideNN/... | 关键对象、标签、上下文 | 裁切窗口 / 比例 / 显示宽高 |

说明使用原始论文图、重新绘制 SVG，还是生成式示意图，以及原因。成对图片明确相同显示尺寸与比例，不拉伸。若本页无图片或裁切，删去无用表格。详细来源与证据边界链接到 `../assets/slideNN/source.md`。

## 图示语义与跨页连续性

- 箭头、坐标轴、符号和颜色分别表示什么（仅在存在这些元素时填写）。
- 与前一页保持不动的元素、大小和锚点。
- 本页新增或替换的注解，以及下一页需要延续的部分。
- 关联布局链接，例如 `slideNN.md`，按实际页号替换。

## 待确认与检查

- 待确认的内容或布局选择。
- 实现后检查裁切、文字可读性、重叠、留白和跨页对齐。
- 若已检查，记录真实结果和仍存在的问题。不要预先勾选通过。

用当前有效方案替换旧决定，避免把互相冲突的修改历史都留在正文中。
