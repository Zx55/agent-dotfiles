#!/usr/bin/env node
console.error("OOXML patch fallback is intentionally empty until a concrete PPTXGenJS gap is identified.");
console.error("Patch only documented, task-specific PPTX XML issues and record the fallback in manifest.json.");
process.exit(2);
